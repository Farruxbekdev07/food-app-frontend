import { Box } from "@mui/material";
import React from "react";
import Card from "../Card";

type Props = {};

const Main = (props: Props) => {
  return (
    <Box>
      <Card />
    </Box>
  );
};

export default Main;
