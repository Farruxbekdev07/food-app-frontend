export enum UserRole {
  user = "user",
  admin = "admin",
  courier = "courier",
}

export enum StatusEnum {
  pending = "pending",
  cooking = "cooking",
  received = "received",
  delivering = "delivering",
}
